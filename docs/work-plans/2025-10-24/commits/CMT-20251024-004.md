## 提交 CMT-20251024-004（规划中）

1. **关联任务**：P1-3（预览尺寸适配）、P1-4（预览模式默认行为）
2. **用户意见**：暂停"002"相关工作，优先处理预览体验优化。确认现有逻辑基本完整，主要需要调整比例和确认实现细节。
3. **开发总结（阶段性规划）**：
   - **需求要点**  
     - ✅ **EXIF方向处理**：图片打开时默认加载正确方向（基于EXIF），上传时EXIF orientation会被处理
     - ✅ **尺寸计算逻辑**：标准模式最长边=显示区域80%，沉浸模式最长边=显示区域90%
     - ✅ **旋转功能**：可持续旋转（不限制360度），无反转动作，纯旋转
     - ✅ **重置机制**：切换图片和关闭预览后都重置（不做记忆，不修改方向）
     - ✅ **默认行为**：点开图片默认进入标准预览模式，不记忆上次沉浸模式状态
     - ✅ **沉浸模式**：沉浸模式下不显示多余按钮，退出沉浸模式有两种情况（退到标准预览/退出预览）
     - ✅ **预览模式逻辑**：沉浸和标准都是预览模式，使用一套逻辑，点击右上角退出预览模式需要复原
   - **方案概述**  
     - 确认现有PhotoPreview组件逻辑基本完整
     - 需要调整imageFit.ts中的比例常量（80%/90%）
     - 需要确认EXIF处理和重置机制的实现细节
     - 优化上传弹窗响应式问题（小屏溢出）
     - 改进背景设计（低饱和渐变，与UI协调）
   - **任务拆解**  
     1. 确认当前实现的比例是否为80%/90%
     2. 验证EXIF orientation处理是否完整
     3. 测试旋转重置机制（切换图片、关闭预览、退出沉浸模式）
     4. 优化上传弹窗响应式（max-h-[90vh]、overflow-y-auto、sticky底部按钮）
     5. 改进预览背景设计（多层径向渐变+低饱和色调）
     6. 自测脚本：桌面/移动、不同方向照片、旋转开关、沉浸/标准切换
   - **当前状态**：现有逻辑基本完整，主要需要调整比例和确认实现细节，等待 Cursor 环境恢复后执行。
4. **审计记录**：2025-10-24 由 Codex 登记规划，待实现阶段再补充审计。
5. **修订历史**：暂无（规划阶段）。
6. **验收结果**：未开始（等待实现与审计）。
7. **Git 提交**：待实现后补充。
